apiVersion: v1
kind: Service
metadata:
  name: myproject1-service
spec:
  type: NodePort
  selector:
    app: myproject
  ports:
    - protocol: TCP
      port: 80  
      targetPort: 8000
      nodePort: 30000 
